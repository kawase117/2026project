{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["hall_hint", "date_hint", "entries"],
  "properties": {
    "hall_hint": {
      "type": ["string", "null"],
      "description": "画像全体の見出しにあるホール名の生表記。複数店舗が併記されていればそのまま。無ければnull"
    },
    "date_hint": {
      "type": ["string", "null"],
      "description": "画像が対象とする営業日。年が読めればYYYY-MM-DD、月日だけならM/D、日付が無ければnull"
    },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["hall_hint", "machine_name", "machine_numbers", "note"],
        "properties": {
          "hall_hint": {
            "type": ["string", "null"],
            "description": "この機種が属する店舗名の生表記。1枚に複数店舗の表が載る場合は必ず各entryに対応する店舗を入れる。店舗が1つだけなら全entryに同じ値を入れる。判別できなければnull",
            "$comment": "画像単位ではなくentry単位でホールを持たせることが必須。台番号がどの店舗のものか失われるため"
          },
          "machine_name": {
            "type": "string",
            "description": "機種名"
          },
          "machine_numbers": {
            "type": "array",
            "description": "台番号文字列の配列。先頭ゼロがあり得るため数値にしない",
            "items": {"type": "string"}
          },
          "note": {
            "type": ["string", "null"],
            "description": "全台系や高設定示唆などの補足。無ければnull"
          }
        }
      }
    }
  }
}
