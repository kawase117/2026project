# Roadmap — 30日計画(2026-07-13起点)

`mirror_evidence.md` のPhase 5(レバレッジ分析)から導出。各項目に受領証と初手アクションを付す。

## 今週中にDROP(停止)

- ホール横断の統一法則探索を正式にクローズ。全9ホールの分散分解で実証済みnull/negative(`signal-ceiling-variance-decomposition`, `kamata7-jug-daily-selection-negative`)。「もしかして今度こそ」の再検証をしない。
- agmsg・`/evolve --generate` は復活検討リストからも外す(既に廃止済み、根拠あり)。

## 今月中にDELEGATE(委任・自動化)

優先順位順、上ほど回収額が大きい:

1. **答え合わせの自動再実行** — DBの欠損データが埋まるたびに手動で「再度確認してください」と頼んでいる(198d0d25)。新規スキル `prediction-recheck` を導入し、DB更新後に一度のコマンドで再検証できるようにする。
2. **instinct-exportの自動発火** — セッション終盤に毎回手動トリガーし、「それ以前の分も」と取りこぼしを都度指摘している。compact-prep実行時にinstinct-exportを自動連動させる運用を検討(hookの追加は別途相談)。
3. **Codexプロンプトの精度チェック** — 契約定義・スキーマ粒度の指摘が毎回2ラウンド発生している(9dec59e9)。新規スキル `codex-prompt-precision` で送信前チェックリストを固定化。
4. **python実行環境の固定** — Windows Storeのダミーエイリアスに毎回引っかかる(559c52dc, 0048da32)。CLAUDE.mdに正しい起動コマンドを1行追記して恒久解決。

## 毎日PROTECT(守る)

- 実装前の重大度タグ付き構造化レビュー(自分・Codex問わず適用)。速度のために簡略化しない。
- ホール別独立分析という設計。プーリングへの誘惑があっても崩さない。
- 発見→原因究明→妥当なら実装、という3ゲート構造。
- トークン予算が理由の意図的保留は正当。ただし保留中も「プランニングだけ」は並行して進める(2-4の機種スペック企画で自己批判済みの改善点)。

## RHYTHM(いつ何をするか)

- 深夜3-5時台・夜20-23時台を新規仮説検証(FOCUS領域)に充てる。もっとも速く質の高い反復が出ている時間帯。
- セッション冒頭15分を固定の「環境点検」枠にする(python経路・DB接続確認)。環境トラブルが分析時間に食い込むのを防ぐ。新規スキル `env-check` を参照。

## 30日後の確認事項

- FOCUS領域(ドメイン直感による仮説創出、現象/予測可能性の切り分け)に使った時間が、WASTE領域(答え合わせ手動再実行・python迷子・プロンプト精度チェック)から回収できたかを見る。
- 「予測精度が上がったら収益化検証に取り組む」の判定基準がまだ言葉になっていない(`mirror.md` 参照)。この30日以内に、その基準を一度でも具体的な数値(Hit@3の閾値など、既存の `prediction-evaluation` スキルにある「30日移動Hit@3が40%超」を流用できる可能性あり)として書き出せるかを確認する。
