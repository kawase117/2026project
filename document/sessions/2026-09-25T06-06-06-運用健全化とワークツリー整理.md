# 運用健全化とワークツリー整理

**日時**: 2026-09-24〜2026-09-25
**セッション**: main（トークン余裕時の保守作業）
**背景**: 「明日の朝までにトークンが余っている」という申し出を受け、積み残しタスクの消化とプロジェクトの構造的な健全化を行った。

## 実装内容

### 1. 引用ツイート画像混入の解消（`project-quoted-image-refetch-pending`）
- `scraper/twitter_monitor/measure_quote_image_hashes.py`（新規、読み取り専用）でハッシュ一致による混入計測を実装。
- 残suspect11件（999999Q9Q 8・kawasakislot 2・sloneko222 1）を`repair_tweet_images.py --apply`で取り直し。
- 作業中に`repair_tweet_images.py`の`strip_emoji`（本文突き合わせ用）の欠陥3種を発見・修正:
  FE0F付きBMP内記号（‼️⬇️等）・キーキャップ絵文字（7️⃣）・FE0Fなし単体BMP絵文字（✨等）。
  修正前は11件中10件が「本文が一致しない」で誤スキップされていた。
- `prefilter.py` → `extract_answers.py --tweet-ids` で再抽出（新規画像は`image_features`が無くJOIN除外される罠を確認）。
- 残suspect（5枚以上の投稿）は現在0件。

### 2. MDE_GATE.mdの日付軸gate列への警告追加
- `backtest/scratch/mde_map_analysis.py`（gitignore対象）に`calendar_reference_diagnostic()`を追加。
- `reference_effect`（calendarクラス）はrakuen_dd_jugという単一claimの値をdd_event/strong_zorome/weekdayの
  3軸・9ホールに転用しており、実測中央値の2.5〜10倍楽観的と判明。
- 観測データから新しいreference値を作ると自己参照になるため、**数値は変更せず**警告文＋楽観度診断表を追加するに留めた。
- ついでにドリフトしていた検収フィクスチャ2件（n_days 613→615、物理軸DETECTABLE件数 32→31）を実測に合わせて更新。

### 3. スキル追加・更新
- `.claude/skills/twitter-monitor-daily/SKILL.md`: 上記1の罠（prefilter未実行での無言0件処理、strip_emoji漏れ、PYTHONUTF8未設定時の文字化け）を追記。
- `.claude/skills/hall-analysis-checklist/SKILL.md`（新規）: `feedback_hall_analysis_standard_passes`（2026-09-11制定）の6パスチェックリストを独立スキル化。従来はメモリ頼みで分析スキルにも入っていなかった。

### 4. Git整理（未コミット分の反映）
- 未コミットだった変更を意味単位で10コミットに分割してpush（prediction_axes.py新規機能、daily-digest hall-review機能、site777メニュー6/7必須化、announce/forward日次データ、instinct5件、EVENT_DAYS/PAST_PROMISES registry）。
- 不要な`.claude/worktrees/`3件（cool-lovelace, funny-lederberg, loving-turing）を削除。うち1件（cool-lovelace）に未コミットの有用な修正（`run_daily_ingest.py`のUTF-8出力強制）があったためcherry-pickしてからworktree削除。
- ローカルブランチ21個→mainのみに整理。実質的な未完了作業が入っていた3件（ML Phase6B・store_optimized_pipelineリファクタ・ml/experiments再構成）は`archive/*`タグとしてorigin側に退避してから削除（ユーザー判断：一旦保留）。

### 5. Deathwatchアラート調査（結論：新規リスクなし）
- `mitoya_jug_corner1`・`rakuen_clean_edge1_jug/bt`のWEAKENED/DEADアラートを調査。
- 楽園側は`document/registry/HYPOTHESIS_LEDGER.md`で**2026-07-28時点で既に「失効」判定済み**（陽性対照として維持されていただけ）と判明。
- `mitoya_jug_edge1`の新規WEAKENEDも、2026-07-31の追試10で既に「位置ベースの投入方針そのものの変更」と解釈済みのパターンの継続。
- 現在稼働中の全forward-testルール（`backtest/prereg/*.json`）はRB確率・機種モデルベースのスコアのみで、位置（edge/corner）に依存するものは無いことを確認。→ **運用リスクなし**。
- `backtest/deathwatch/claims.json`のnoteに「失効済み・陽性対照」である旨を追記し、STATUS.md/status.csvを`--recent-days 180`で再生成。

### 6. instinct TTL自動退役の有効化
- `scripts/compile_instincts.py`に`--ttl-days`オプションは実装済み（「108件中105件が訂正対象を名指しし忘れて何も退役しない」という既知問題への対策、スクリプト自身のdocstringに記録）だったが、`instinct-export`スキルの実行コマンドに付いておらず**機能していなかった**。
- `verification_status: confirmed`が付いているのは432件中2件のみ。
- ユーザー判断で**180日TTLを有効化**。`.claude/skills/instinct-export/SKILL.md`のコマンドに`--ttl-days 180`を追加し、実際にコンパイル・push済み。

## 主要な発見

- **プロジェクトのデータ収集・自動化インフラは既に厚く、「収集したものを閉じるループ」が薄い**という構造的な傾向を確認。今回発見した3件（instinct TTL未使用、deathwatch noteの経緯欠落、日付軸gate列の誤用リスク）はいずれも「仕組みは作ったが最後の一手が抜けていた」パターン。
- Deathwatchの「新規アラート」は多くが既に過去（7月）に解釈済みの現象の再確認であり、過去の調査記録（HYPOTHESIS_LEDGER.md）を先に確認せずに再調査するとムダになる。→ アラート対応時は必ずHYPOTHESIS_LEDGERを先に検索すべき。
- `.claude/worktrees/`配下に古いworktreeが残っている場合、mainに既にマージ済みでも実体削除がOS側のファイルロック（"Device or resource busy"）で失敗することがある。他プロセスが使用中の可能性があるため強制削除は避けるべき。

## 次のステップ

- `archive/*`タグに退避した3件のML作業（fervent-golick=Phase6B、infallible-visvesvaraya=store_optimized_pipeline、sad-cori=ml/experiments再構成）の再開/破棄判断は保留中。思い出したタイミングで判断すること。
- instinct TTL 180日は今後の運用で実際に機能するか、次回のinstinct-export実行時に確認する。
- `.claude/worktrees/loving-turing-710862`のフォルダ実体は削除できず残ったまま（git上は登録解除済みなので実害なし）。

---
**記録者**: Claude Sonnet 5
**ステータス**: ✅ 完了
